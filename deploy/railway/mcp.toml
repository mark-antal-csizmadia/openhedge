[build]
builder = "DOCKERFILE"
dockerfilePath = "openhedge-core/Dockerfile"

[deploy]
startCommand = "python -m openhedge_core.mcp_server"
healthcheckPath = "/health"
healthcheckTimeout = 300
numReplicas = 1
restartPolicyType = "ON_FAILURE"
