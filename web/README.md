# Landing page

Next.js + shadcn site for [openhedge.app](https://openhedge.app). Isolated from `openhedge-core` — do not add this app to Docker Compose or the OSS Railway project.

```bash
npm install
npm run dev
```

Deploy with [`.agents/skills/how-to-deploy-landing/SKILL.md`](../.agents/skills/how-to-deploy-landing/SKILL.md).
